export enum ProductRibbonProps {
  Deal = "DEAL",
  Featured = "FEATURED",
  LastMinute = "LAST_MINUTE",
  Top = "TOP", // Rolled back to TOP because the LMD label not showing up when product has both LMD and TOP_PRODUCT
}
